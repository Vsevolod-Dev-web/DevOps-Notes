По умолчанию `grep` читает только[ стандартный вывод (1)](Дескриптор). Вывод в (2) дескрипторе она не видит.

Чтобы отследить сообщение ошибок, можно направить второй дескриптер (2) в первый (1).
```bash
ls /root 2>&1 | grep "Denied"
```
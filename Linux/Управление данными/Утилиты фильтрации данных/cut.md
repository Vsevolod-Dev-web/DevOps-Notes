``cut`` - позволяет извлекать "вырезать" определенные части - байтов, символов или полей (столбцов) - из каждой строки файлов или стандартного ввода.

Команда обрабатывает текст построчно, используя разделитель (по умолчанию - табуляцию).
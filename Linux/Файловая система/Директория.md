Директория (Каталог) - это таблица "имя файла" -> номер [inode](inode).

Имя файла хранится в директории, в inode его нету. Отсюда вытекает интересная вещь: на один inode может указывать несколько имён - это называется жёсткая ссылка (hard links);
В [ядре](Ядро) Linux куча `struct {...}`. Это абстрактное описание части системы. Например - файл.

Структура Файл содержит определенные поля: inode, имя_файла. Каждый файл - это крутящийся в памяти экземпляр этой структуры с определенными значениями полей.
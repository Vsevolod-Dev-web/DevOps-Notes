---
"Дата:": 2026-03-05 20:44
---
`su - vsevolod`. Команда **su** - это команда переключения на другого пользователя. `su - [user_name]`. При переключении меняется: окружение, доступы, домашняя директория того пользователя на которого ты переключаешься. Так остаётся пока не напишешь `exit`

```bash
su - anna # Стал Анной. Всё что делаю дальше — от её имени
exit # Вернулся к себе
```

### !!!
Если `su` используется без передачи аргумента, то ты автоматически переключаешься на **root**.

# chown 
`chown` - команда для установки роли на файл.

Пример:
```bash
sudo chown dispetcher:dispetcher /home/dispt/chromExt
```

`chown -R` - для рекурсивного изменения роли.
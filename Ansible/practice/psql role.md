	Нужно поставить PostgreSQL на сервер с нуля.

### Точка закрытия?
- На таргете стоит PSQL (Установлены все необходимые пакеты, бинарник работает)
- Кластер в рабочем состоянии (есть база, хотя бы один юзер).
- К базе можно подключиться (порт открыт, pg_hba.conf прописан)

### Одна роль или несколько?
Сделаю одну роль, т.к. все равно разделения будут очень специфичны и это будет over.

- psql-init:
	- Установить пакеты
	- Проверка работы кластера.
	- Тюнинг конфигов pg_hba.conf, postgresql.conf
	- Ребут после правки кофнигов (handler)
	- Проверка доступности кластера. (через postgres).

Создание юзера и бд это уже отдельная задача, сейчас не выполняю

### Tasks
	main.yaml мастер-таска

	Установка пакетов

	Проверка работы кластера (Поднят ли демон сервера который держит кластер ansible.builtin.service: state: started)

	Таска на отправку конфигов pg_hba.conf и postgresql.conf
Замодить права и группы для pg_hba

	Вызов Handler на ребут службы после тюнинга конфигов (через ansible.builtin.meta: flush_handlers)

	Проверка доступности кластера: подключится с postgres ??? Нужна база ??? Нужен ли этот таск? может просто проверить демона кластера опять?.

### Templates
	pg_hba.conf.j2

	postgresql.conf.j2

Точно нужен шаблон на hba, не уверен на счёт postgresql.conf. 

### Handlers
	psql-reboot

### Defaults n Vars
Порт на базу. (Похуй, оставляем стандартный )
Listen host (откуда слушать подключения (откуда tcp прокладывать))
Переменные для подстановки в hba.conf


# Master-Playbook
Вызов роли, передача параметров, указание хоста.


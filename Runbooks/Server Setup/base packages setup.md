Для моего софта, со стеком из python, node.js необходимы библиотеки

- node.js
- npm
- [python3 venv](Python%20venv)

```bash
#to setup a venv, install python3 venv
python -m venv venvName
#then, in dev dir, create venv:
cd ./app && source /.venv/bin/activate
```


На сервере стоит psql, подключен к базе app1, с юзером app1user, паролем app1user.

См. pg_hba.conf (конфиг правил подключения к СУБД (откуда, к какой базе, с каких адресов)

Способы диагностики:
1. См. systemctl
2. См. journalctl *(Стоит понимать, что не все службы охотно отдают логи в ctl. Всегда нужно смотреть в варлоге)*
3. См. /var/log/postgresql
4. Пробуем воспроизвести действия, при наличии смотрим, что отдаёт.

Проблема была - в указанном выше конфиге правила блокировали все подключения кроме юзера postgresql